# Env-scoped header set consumed by Headers.fromProperties("headers/qa.properties")
# Anything you set here is merged onto the running header map; per-test builder
# calls after .fromPropertiesFile(...) override these on conflict.
X-Env=qa
X-Api-Key=qa-api-key-abc123
X-Tenant-Id=acme-qa
X-Client-Name=api-automation-restassured
